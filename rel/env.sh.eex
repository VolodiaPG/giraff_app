#!/bin/sh

export ERL_FLAGS="+sbtu +A0 +B +scl false +P 1000000 +K true -noinput -proto_dist Elixir.Giraff.Epmd.Dist -start_epmd false -epmd_module Elixir.Giraff.Epmd.Client -kernel prevent_overlapping_partitions false"
export RELEASE_DISTRIBUTION="name"
export RELEASE_NODE="${NAME}-${OPENED_PORT}@${PRIVATE_IP}"
echo "$RELEASE_NODE"
