#!/bin/sh

export ERL_FLAGS="-proto_dist Elixir.Giraff.Epmd.Dist -start_epmd false -epmd_module Elixir.Giraff.Epmd.Client -kernel prevent_overlapping_partitions false"
export RELEASE_DISTRIBUTION="name"
export RELEASE_NODE="${NAME}-${OPENED_PORT}@${PRIVATE_IP}"
echo "$RELEASE_NODE"
